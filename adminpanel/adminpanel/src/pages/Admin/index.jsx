
const Admin = ({suppliers}) => {
  console.log(suppliers);
  return (
    <ul>
      {/* {suppliers?.map((elem) => {
        return <li>{elem.id}</li>;
      })} */}
    </ul>
  );
};

export default Admin;
